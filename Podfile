platform :ios, '11.0'

target 'CampusTour' do
  use_frameworks!

  pod 'SnapKit', '~> 4.0.0'
end

plugin 'cocoapods-keys', {
    :project => "CampusTour",
    :keys => [
        "GoogleMapsAPI"
    ]
}
